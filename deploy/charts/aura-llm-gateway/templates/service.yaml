apiVersion: v1
kind: Service
metadata:
  name: {{ include "aura-llm-gateway.fullname" . }}
  labels:
    {{- include "aura-llm-gateway.labels" . | nindent 4 }}
  {{- with .Values.service.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "aura-llm-gateway.selectorLabels" . | nindent 4 }}
