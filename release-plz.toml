# release-plz configuration
# https://release-plz.ieni.dev/docs/config

[workspace]
# Git tag format for releases
git_tag_name = "v{{ version }}"
# Enable GitHub releases
git_release_enable = true
# Changelog update strategy
changelog_update = true
# Changelog configuration
changelog_config = "cliff.toml"
# Release name for workspace releases
git_release_name = "Aura LLM Gateway v{{ version }}"

# Allow dirty worktree for release commits
allow_dirty = false

# Semver check disabled (not publishing to crates.io)
semver_check = false

# Individual package configuration
# publish = false prevents publishing to crates.io
# Only aura-proxy creates releases (main binary)
[[package]]
name = "aura-types"
publish = false
release = false

[[package]]
name = "aura-db"
publish = false
release = false

[[package]]
name = "aura-core"
publish = false
release = false

[[package]]
name = "aura-proxy"
publish = false
# This is the main package - enable releases for it
release = true
git_release_enable = true
